import React from 'react';
import PropTypes from 'prop-types';

const propTypes = {};

const defaultProps = {};

const LodgingScreen = ({}) => {
  return <h1>Logement</h1>;
};

LodgingScreen.propTypes = propTypes;
LodgingScreen.defaultProps = defaultProps;

export default LodgingScreen;
